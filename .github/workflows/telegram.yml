- name: Check BOT_TOKEN
  env:
    BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
  run: |
    if [ -z "$BOT_TOKEN" ]; then
      echo "❌ BOT_TOKEN is NOT set"
      exit 1
    else
      echo "✅ BOT_TOKEN is set"
      echo "Length: ${#BOT_TOKEN}"
    fi
